# Project IKEA: Аналіз даних та Прогнозування цін на товари 

## 🎯 Огляд Проєкту

Цей проєкт є комплексним дослідженням даних товарів IKEA (станом на листопад 2020 року) \. Основна мета — **виконати глибокий дослідницький аналіз даних (EDA)**, **перевірити статистичні гіпотези** щодо факторів ціноутворення та **розробити модель машинного навчання (ML)** для точного прогнозування ціни товару.

## ✨ Ключові Етапи та Результати

### 1. Збір та Очищення Даних

Набір даних містив 3694 записи та 14 стовпців \, \.

*   **Очищення:** Видалено зайвий службовий стовпець (`'Unnamed: 0'`) \.
*   **Видалення дублікатів:** Виявлено та видалено дублікати на основі унікального ідентифікатора товару (`item_id`), що зменшило набір даних до **2962 унікальних товарів** \, \, \.
*   **Інженерія ознак:**
    *   Створено похідну ознаку об'єму (`size`) як добуток розмірів (`height * width * depth`) \.
    *   Проведена **імпатація (заповнення пропусків)** у стовпцях розмірів (`depth`, `height`, `width`) за допомогою **медіани** відповідної `category` \, \.
    *   Створено агреговані ознаки: **медіанна ціна за категорією** (`category_median_price`) та **медіанна ціна за дизайнером** (`designer_median_price`) \, \.
*   **Трансформація:** Ціна (`price`) була **логарифмована** (`price_ln`) для нормалізації асиметричного розподілу даних \, \.

### 2. Дослідницький Аналіз Даних 

Аналіз включав візуалізацію розподілу цін (логарифмованих та нелогарифмованих) \, \, розподіл товарів за категоріями \, \, та залежність ціни від категорії та дизайнера \, \, \, \, \.

**Ключові Висновки EDA:**
*   Розподіл цін сильно скошений, що вимагало логарифмічного перетворення для моделювання \, \.
*   Найдорожчі товари за медіанною ціною належать до категорій `Tables & desks` та `Sofas & armchairs` \, \.
*   Висока кореляція спостерігається між розмірами товару (`depth`, `height`, `width`) та ціною \.

### 3. Перевірка Статистичних Гіпотез

Проведено перевірку гіпотез щодо впливу текстових та бінарних ознак на ціну товару:

| Гіпотеза | Тести | Результат |
| :--- | :--- | :--- |
| **Вплив довжини назви (`name_length`) на ціну?** | Кореляція, OLS-регресія, T-test, Mann-Whitney U, Bootstrap \, \, \, \, \, \ | **Статистично значущий, але слабкий.** Коефіцієнт кореляції становить 0.146691 \. OLS-модель має R-squared лише 0.022, що означає, що довжина назви мало пояснює варіацію ціни \, \, \. T-test та Mann-Whitney U не дозволили відкинути нульову гіпотезу на рівні 0.05 \, \. |
| **Чи впливає наявність інших кольорів (`other_colors`) на ціну?** | Кореляція, T-test, Mann-Whitney U, Kruskal-Wallis, ANOVA, Tukey HSD, Shuffle test \, \, \, \, \, \, \ | **Статистично значущий вплив.** Усі використані тести (зокрема, T-test, Kruskal-Wallis та Tukey HSD) показали, що є значна різниця в середніх цінах товарів, які мають інші кольори, порівняно з тими, що не мають \, \, \, \. Тест Tukey HSD показав, що різниця в середніх цінах становить **-284.31** \, \. |

### 4. Моделювання (Machine Learning)

Проєкт зосереджено на завданні **прогнозування ціни (регресія)**.

**Використані моделі:** Linear Regression, LassoCV, RidgeCV, SVR, KNeighborsRegressor, DecisionTreeRegressor, RandomForestRegressor, GradientBoostingRegressor \.

**Найкращі результати (на тестовому наборі):**

| Модель | R² Score | MAE (Середня Абсолютна Помилка) | RMSE (Корінь із Середньоквадратичної Помилки) |
| :--- | :--- | :--- | :--- |
| **RandomForestRegressor (RF) (оптимізована)** | **0.82940** \ | **342.07478** \ | **566.86280** \ |
| RandomForestRegressor (базова) | 0.81429 \ | 346.71953 \ | 591.42637 \ |
| GradientBoostingRegressor | 0.78394 \ | 398.97738 \ | 637.92007 \ |

**Оптимізація та Важливість Ознак (RandomForest):**
*   Модель **RandomForestRegressor** була оптимізована за допомогою **GridSearchCV** та **KFold крос-валідації** \, \.
*   Ключові фактори, що впливають на ціну (Feature Importance) \, \:
    1. **`width_1`** (ширина) – **0.38738**
    2. **`designer_median_price`** (медіанна ціна дизайнера) – **0.30316**
    3. **`depth_1`** (глибина) – **0.13977**

## 🛠️ Технічний Стек (Requirements)

Для запуску проєкту необхідні наступні бібліотеки (перелік на основі імпортованих модулів) \, \:

*   `pandas`, `numpy`, `re`
*   `matplotlib`, `seaborn`, `plotly.express` (для візуалізації) \, \
*   `scipy.stats`, `statsmodels` (для статистичних тестів) \
*   `scikit-learn` (включаючи `Pipeline`, `ColumnTransformer`, `GridSearchCV`, різні регресори та препроцесори) \, \
*   `requests`, `os` (для завантаження даних) \, \


    ```
2.  Відкрийте файл `Project_IKEA.ipynb` (за допомогою Jupyter Notebook, JupyterLab або VS Code).
3.  Виконайте послідовно всі комірки для відтворення процесу: за
вантаження даних \, очищення, EDA, перевірки гіпотез та моделювання ціни.
<img width="2752" height="1536" alt="unnamed" src="https://github.com/user-attachments/assets/96a616ff-8342-4ecf-a85f-dd070dd10745" />
